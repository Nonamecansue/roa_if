add_namespace = SE_test.8


country_event = { 
	
	#debuging,remove this later	
	id = SE_test.8
    title = SE_test.8.t
    desc = SE_test.8.d 
	is_triggered_only = yes
	hidden = no 
	
	
	option = { 
		#how it looks when sth. was not defined properly
		name = SE_test.8.f
		hidden_effect = {
			if = {
				limit = { is_ai = no }
				set_variable = { Active_Superevent_ID = 27 }
				if = {
					limit = {
						NOT = { has_country_flag = PB_disable_superevent_sound }
					}
					meta_effect = {
						text = {
							scoped_play_song = "Super_Event_Sound_27"
						}
						ID = "[?27]"
					}
				}
			}
		}
	}

	option = { 
		#how it looks when sth. was not defined properly
		name = SE_test.8.g
		hidden_effect = {
			if = {
				limit = { is_ai = no }
				set_variable = { Active_Superevent_ID = 28 }
				if = {
					limit = {
						NOT = { has_country_flag = PB_disable_superevent_sound }
					}
					meta_effect = {
						text = {
							scoped_play_song = "Super_Event_Sound_28"
						}
						ID = "[?28]"
					}
					
				}
			}
		}
	}

	option = { 
		#how it looks when sth. was not defined properly
		name = SE_test.8.h
		hidden_effect = {
			if = {
				limit = { is_ai = no }
				set_variable = { Active_Superevent_ID = 29 }
				if = {
					limit = {
						NOT = { has_country_flag = PB_disable_superevent_sound }
					}
					meta_effect = {
						text = {
							scoped_play_song = "Super_Event_Sound_29"
						}
						ID = "[?29]"
					}
				}
			}
		}
	}

option = { 
		#how it looks when sth. was not defined properly
		name = SE_test.8.i
		
        
		hidden_effect = {

			HRP = {
			set_cosmetic_tag = HRP_end
			retire_country_leader = yes
			every_country = {
            limit = {
                NOT = {
                    has_country_tag = HRP
                }
            }
            annex = PREV
		}
			if = {
				limit = { is_ai = no }
				set_variable = { Active_Superevent_ID = 30 }
				if = {
					limit = {
						NOT = { has_country_flag = PB_disable_superevent_sound }
					}
					meta_effect = {
						text = {
							scoped_play_song = "Super_Event_Sound_30"
						}
						ID = "[?30]"
					}
				}
			}
		}
	}

	

}

