add_namespace = SE_test.4


country_event = { 
	
	#debuging,remove this later	
	id = SE_test.4
    title = SE_test.4.t
    desc = SE_test.4.d 
	is_triggered_only = yes
	hidden = no 
	
	
	option = { 
		#how it looks when sth. was not defined properly
		name = SE_test.4.f
		hidden_effect = {
			if = {
				limit = { is_ai = no }
				set_variable = { Active_Superevent_ID = 11 }
				if = {
					limit = {
						NOT = { has_country_flag = PB_disable_superevent_sound }
					}
					meta_effect = {
						text = {
							scoped_play_song = "Super_Event_Sound_11"
						}
						ID = "[?11]"
					}
				}
			}
		}
	}

	option = { 
		#how it looks when sth. was not defined properly
		name = SE_test.4.g
		hidden_effect = {
			if = {
				limit = { is_ai = no }
				set_variable = { Active_Superevent_ID = 12 }
				if = {
					limit = {
						NOT = { has_country_flag = PB_disable_superevent_sound }
					}
					meta_effect = {
						text = {
							scoped_play_song = "Super_Event_Sound_12"
						}
						ID = "[?12]"
					}
				}
			}
		}
	}

	option = { 
		#how it looks when sth. was not defined properly
		name = SE_test.4.g
		hidden_effect = {
			if = {
				limit = { is_ai = no }
				set_variable = { Active_Superevent_ID = 13 }
				if = {
					limit = {
						NOT = { has_country_flag = PB_disable_superevent_sound }
					}
					meta_effect = {
						text = {
							scoped_play_song = "Super_Event_Sound_13"
						}
						
						ID = "[?13]"
					}
				}
			}
		}
	}

}

