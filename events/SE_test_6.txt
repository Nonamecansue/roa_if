add_namespace = SE_test.6


country_event = { 
	
	#debuging,remove this later	
	id = SE_test.6
    title = SE_test.6.t
    desc = SE_test.6.d 
	is_triggered_only = yes
	hidden = no 
	
	
	option = { 
		#how it looks when sth. was not defined properly
		name = SE_test.6.f
		hidden_effect = {
			if = {
				limit = { is_ai = no }
				set_variable = { Active_Superevent_ID = 19 }
				if = {
					limit = {
						NOT = { has_country_flag = PB_disable_superevent_sound }
					}
					meta_effect = {
						text = {
							scoped_play_song = "Super_Event_Sound_19"
						}
						ID = "[?19]"
					}
				}
			}
		}
	}

	option = { 
		#how it looks when sth. was not defined properly
		name = SE_test.6.g
		hidden_effect = {
			if = {
				limit = { is_ai = no }
				set_variable = { Active_Superevent_ID = 20 }
				if = {
					limit = {
						NOT = { has_country_flag = PB_disable_superevent_sound }
					}
					meta_effect = {
						text = {
							scoped_play_song = "Super_Event_Sound_20"
						}
						ID = "[?20]"
					}
				}
			}
		}
	}

	option = { 
		#how it looks when sth. was not defined properly
		name = SE_test.6.h
		hidden_effect = {
			if = {
				limit = { is_ai = no }
				set_variable = { Active_Superevent_ID = 21 }
				if = {
					limit = {
						NOT = { has_country_flag = PB_disable_superevent_sound }
					}
					meta_effect = {
						text = {
							scoped_play_song = "Super_Event_Sound_21"
						}
						ID = "[?21]"
					}
				}
			}
		}
	}

	option = { 
		#how it looks when sth. was not defined properly
		name = SE_test.6.i
		hidden_effect = {
			if = {
				limit = { is_ai = no }
				set_variable = { Active_Superevent_ID = 22 }
				if = {
					limit = {
						NOT = { has_country_flag = PB_disable_superevent_sound }
					}
					meta_effect = {
						text = {
							scoped_play_song = "Super_Event_Sound_22"
						}
						ID = "[?22]"
					}
				}
			}
		}
	}

}

