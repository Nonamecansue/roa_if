add_namespace = SE_test.3


country_event = { 
	
	#debuging,remove this later	
	id = SE_test.3
    title = SE_test.3.t
    desc = SE_test.3.d 
	is_triggered_only = yes
	hidden = no 
	
	
	option = { 
		#how it looks when sth. was not defined properly
		name = SE_test.3.f
		hidden_effect = {
			if = {
				limit = { is_ai = no }
				set_variable = { Active_Superevent_ID = 9 }
				if = {
					limit = {
						NOT = { has_country_flag = PB_disable_superevent_sound }
					}
					meta_effect = {
						text = {
							scoped_play_song = "Super_Event_Sound_9"
						}
						ID = "[?9]"
					}
				}
			}
		}
	}

	option = { 
		#how it looks when sth. was not defined properly
		name = SE_test.3.g
		hidden_effect = {
			if = {
				limit = { is_ai = no }
				set_variable = { Active_Superevent_ID = 10 }
				if = {
					limit = {
						NOT = { has_country_flag = PB_disable_superevent_sound }
					}
					meta_effect = {
						text = {
							scoped_play_song = "Super_Event_Sound_10"
						}
						ID = "[?10]"
					}
				}
			}
		}
	}

	option = { 
		#how it looks when sth. was not defined properly
		name = SE_test.3.h
		hidden_effect = {
			if = {
				limit = { is_ai = no }
				set_variable = { Active_Superevent_ID = 14 }
				if = {
					limit = {
						NOT = { has_country_flag = PB_disable_superevent_sound }
					}
					meta_effect = {
						text = {
							scoped_play_song = "Super_Event_Sound_14"
						}
						ID = "[?14]"
					}
				}
			}
		}
	}

	option = { 
		#how it looks when sth. was not defined properly
		name = SE_test.3.i
		hidden_effect = {
			if = {
				limit = { is_ai = no }
				set_variable = { Active_Superevent_ID = 15 }
				if = {
					limit = {
						NOT = { has_country_flag = PB_disable_superevent_sound }
					}
					meta_effect = {
						text = {
							scoped_play_song = "Super_Event_Sound_15"
						}
						ID = "[?15]"
					}
				}
			}
		}
	}




}

